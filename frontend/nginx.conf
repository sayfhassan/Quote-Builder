server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # React Router uses client-side routes (e.g. /quotes/:id) that don't exist
  # as real files, so unknown paths fall back to index.html.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
